# System Design

## Architecture

LifeFlow состоит из двух основных частей:

- Frontend — пользовательский интерфейс.
- Backend — серверная часть, API и работа с базой данных.

Frontend взаимодействует с Backend через HTTP API.

Backend хранит данные в базе данных и возвращает их Frontend в формате JSON.